{
  "name": "tsoha-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "heroku-postbuild": "npm run tsc && npm run build:ui",
    "dev": "NODE_ENV=development ts-node-dev server/index.ts",
    "start": "NODE_ENV=production node dist/index.js",
    "build:ui": "cd client && npm install && npm run build --prod && cp -r build ../dist/",
    "build:full": "rm -rf dist && npm run tsc && npm run build:ui",
    "logs:prod": "heroku logs --tail",
    "lint": "eslint --ext .ts server/."
  },
  "engines": {
    "node": "15.11.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.5.1",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.35",
    "@types/pg": "^7.14.11",
    "@typescript-eslint/eslint-plugin": "^4.18.0",
    "@typescript-eslint/parser": "^4.18.0",
    "eslint": "^7.22.0",
    "eslint-config-airbnb-typescript": "^12.3.1",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.3"
  }
}
